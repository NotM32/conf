{ config, pkgs, lib, ... }:

/* Opinionated module to define the disk layout of a system */

with lib;

let
  cfg = config.system.storage;
in {

  imports = [ ];

  options = {

  };

  config = {

  };
}
